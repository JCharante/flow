function errorAlert(message, length) {
	Materialize.toast(message, length, 'rounded red accent-4');
}

function successAlert(message, length) {
	Materialize.toast(message, length, 'rounded light-green accent-4');
}
